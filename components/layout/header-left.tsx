import { View, Text } from 'react-native';
import { Search } from '../ui/icons'

const HeaderLeft = () => {
    return(
        <View>
            <Search />
        </View>
    )
};
export default HeaderLeft;